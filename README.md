### YARRC - Yet Another React Redux Counter

This is a very simple example on how to dispatch actions to reducers and maintain a global state using Redux in React.

## Usage

run `npm install` then `npm start`